version: '3.8'
services:
  unir-local-uno:
    restart: always
    hostname: unir-local-uno
    image: unir-local-uno:latest
    ports:
      - "80:80"
      - "3000:3000"
    volumes:
      - ./nginx.conf:/etc/nginx/nginx.conf:ro
    entrypoint: ["pm2-runtime", "/app/server.js", "-i", "max"]